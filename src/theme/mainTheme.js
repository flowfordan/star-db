import {
    createTheme,
  } from '@mui/material/styles';

  const mainTheme = createTheme({
    palette: {
        primary:{
            main:'#465163',
        }
    }
  });

  export default mainTheme
